/**
 * Balar Linter Test Examples
 *
 * Test examples have been reorganized into separate folders by rule.
 * See README.md for details.
 *
 * Structure:
 * - rule-1-must-be-in-context/  - Tests for "must be in context" rule
 * - rule-2-no-conditionals/     - Tests for "no conditionals" rule
 *
 * Each example is in its own folder with descriptive names:
 * - pass-* folders contain valid examples that should not error
 * - fail-* folders contain invalid examples that should error
 */

export {};
